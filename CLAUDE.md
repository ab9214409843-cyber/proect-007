# CLAUDE.md — Proect_007

Контекст проекта для Claude Code. Читается автоматически в начале каждой сессии в этой папке.

## О проекте

**Proect_007** — веб-приложение (SaaS) для организаторов спортивных мероприятий.
Автор и заказчик — Андрей Башкатов (АНО «ФЦ БАС»). Андрей **не разработчик**: код пишет в основном
Claude, Андрей принимает решения, тестирует «удобно/неудобно» и заводит аккаунты/доступы.

Что приложение должно уметь (MVP):
- создать мероприятие;
- автоматически получить план подготовки (задачи по дате мероприятия);
- вести задачи;
- хранить документы;
- фиксировать опыт после мероприятия.

Полная дорожная карта — в [docs/roadmap.md](docs/roadmap.md). Источник истины по объёму MVP.

## Стек и статус окружения

Стек: **Next.js + TypeScript + Tailwind CSS + Supabase (БД, авторизация, файлы) + Vercel (деплой)**.
Подробнее и почему так — в [docs/stack.md](docs/stack.md).

Статус окружения (обновлено 2026-06-09):
- ✅ **Node.js** — установлен (v24.16.0 LTS), npm 11.13.0.
- ✅ **Git** — установлен (2.54.0).
- ✅ **GitHub** — аккаунт есть.
- ✅ **Supabase** — аккаунт есть; проект `proect-007` создан (регион Central EU / Frankfurt).
- ✅ **Vercel** — подключён к GitHub-репозиторию, сайт развёрнут: https://proect-007.vercel.app
  (Root Directory = `app`, переменные Supabase добавлены; автодеплой при каждом push в `main`).
- ✅ **MCP-расширения** — настроены в `~/.claude.json`: `supabase` (управление БД проекта
  `kjnwpgvldrnidabumhjf`) и `playwright` (браузер для проверки UI). Активируются после перезапуска Claude Code.

## Структура папок

```
Proect_007/
├── CLAUDE.md         ← этот файл, главный контекст и прогресс
├── README.md         ← короткая шапка «с чего начать»
├── docs/             ← дорожная карта, стек, черновик схемы БД
├── app/              ← САМО приложение Next.js 16 (src/app — страницы, src/lib/supabase — клиенты)
├── _шаблоны/         ← библиотека шаблонов документов (положение, смета, ТЗ, отчёт…)
└── Этап_01.._11/     ← по папке на каждый этап: README с задачами, чек-листом, статусом
```

Важно: **код приложения живёт ТОЛЬКО в `app/`** одной общей структурой. Папки `Этап_XX` — это
документация и чек-листы, в них код не кладём.

## Как мы работаем

- **Новое окно на каждую задачу.** Открываешь папку нужного этапа → читаешь её `README.md` →
  выполняешь чек-лист → отмечаешь статус.
- Claude в любом окне внутри `Proect_007` автоматически видит этот `CLAUDE.md` → контекст ясен сразу.
- Общий прогресс отмечаем здесь, в разделе «Текущий этап» (ниже).
- Стиль общения с Андреем: **русский, на «ты», дружески, кратко и по делу, без технического жаргона.**
  Объясняем без терминов, даём конкретные пошаговые инструкции.

## Текущий этап / следующий шаг

- **Этап 1 (фундамент) — завершён ✅.** Next.js 16 в `app/`, Supabase, регистрация/вход/выход, защита кабинета.
- **Сейчас:** Этап 2 (структура продукта) — **завершён ✅**. Публичная главная переписана в смелом
  продуктовом стиле (референс — manifestos.com): hero, проблема, «как это работает», возможности, для
  кого, призыв к действию. Все разделы кабинета сделаны как каркасы и объединены в группу маршрутов
  `app/src/app/(cabinet)/` с общим layout (боковое меню + шапка с выходом): `/dashboard`, `/events`,
  `/events/[id]`, `/tasks`, `/documents`, `/experience`. Защита входом расширена на все разделы
  (`app/src/lib/supabase/session.ts`). Проверено сборкой и end-to-end через браузер.
- **Код на GitHub:** `https://github.com/ab9214409843-cyber/proect-007` (ветка `main`, remote `origin`).
- **Живой сайт:** https://proect-007.vercel.app — развёрнут на Vercel, автодеплой при push в `main`.
- **Следующий шаг:** **Этап 3** — база данных (таблицы `users`, `events`, `tasks`, `documents`,
  `checklist_templates`, `experience_notes`) в Supabase. См. [docs/roadmap.md](docs/roadmap.md).
- Подтверждение email в Supabase сейчас **отключено** (удобно для разработки) — **вернуть перед публичным запуском**.

## Чек-лист готовности к коду

- [x] **Node.js LTS** — v24.16.0.
- [x] **Git** — 2.54.0; репозиторий инициализирован, есть коммиты.
- [x] **Supabase** — проект `proect-007` (Frankfurt), подключён в приложении.
- [x] **Vercel** — аккаунт есть (через GitHub). Деплой подключим, когда зальём код на GitHub.
- [x] **MCP** (Supabase + браузер) — активны, проверены в работе.

### Технические заметки (важно для будущих окон)
- **Next.js 16, не 15.** Есть breaking changes (см. `app/AGENTS.md`): middleware → **`proxy.ts`**
  (функция `proxy`, runtime Node.js); `cookies()`, `params`, `searchParams` — **асинхронные** (через `await`);
  Turbopack по умолчанию; `next lint` убран. Перед правкой Next-кода сверяйся с `app/node_modules/next/dist/docs/`.
- **Supabase подключён через `@supabase/ssr`** (актуальная библиотека, не устаревшие auth-helpers):
  `app/src/lib/supabase/{client,server,session}.ts`, защита маршрутов — `app/src/proxy.ts`.
- **Секреты:** ключи Supabase в `app/.env.local` (под `.gitignore`, в git НЕ попадают).
  Access token Supabase — в `C:\Users\ab921\.claude.json` (вне проекта).
- **Деплой на Vercel:** при подключении указать **Root Directory = `app`** (приложение вложено в подпапку).
- Node/npm/git ставились внутри открытого VSCode → после установки нужен был перезапуск VSCode (PATH).
- MCP-серверы на Windows запускаются через `cmd /c npx ...`.

## Связь с методичкой ФЦ БАС (важно для Этапа 5)

Этап 5 — автоматический план подготовки (задачи по дате мероприятия). Шаблон задач строим не на
абстрактном примере, а на **реальных этапах из методички**:
`C:\Claude_Code\methodology\methodology.md` (9-этапный процесс организации мероприятий ФЦ БАС).
Так продукт сразу станет рабочим, а не учебным.

## Приоритеты MVP / что НЕ делаем

**Обязательно для MVP:** авторизация, главная страница, личный кабинет, мероприятия, автосоздание
задач, задачи, документы, база опыта.

**НЕ делаем в MVP:** мобильное приложение, билеты, регистрацию участников, CRM, маркетинг, сложную
аналитику, интеграции, оплату подписки, AI-генерацию документов. (Эти пункты — в Этап 11 / версию 1.1.)

## Окружение

Платформа: Windows 11, оболочка — PowerShell (не bash). Папка проекта **под git** (ветка `main`,
есть коммиты). Связку с GitHub заведём отдельным шагом (нужен `gh` или ручное создание репозитория).
