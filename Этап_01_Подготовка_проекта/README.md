# Этап 1. Подготовка проекта

## Цель
Создать проект-приложение на Next.js + TypeScript + Tailwind, подключить Supabase и добиться, чтобы
приложение запускалось локально. Настроить авторизацию (вход по email/паролю).

## Задача для Claude (новое окно)
> Создай проект веб-приложения на Next.js, TypeScript и Tailwind CSS в папке `app/`. Сделай структуру
> SaaS-приложения с публичной главной страницей, страницами входа/регистрации и личным кабинетом.
> Затем добавь Supabase, настрой авторизацию через email/пароль; после входа — переход в `/dashboard`.

## Сначала подготовить окружение
Без этого код не запустить (см. [../docs/stack.md](../docs/stack.md)):
- [x] Установить Node.js LTS — готово, v24.16.0 (npm 11.13.0).
- [x] Установить Git — готово, 2.54.0.
- [x] Завести Supabase, создать проект — готово (`proect-007`, Frankfurt).
- [x] Завести Vercel (через GitHub) — готово.
- [x] Подключить MCP: Supabase + браузер — готово (активны после перезапуска Claude Code).

## Чек-лист этапа
- [x] Создан Next.js-проект в `app/` (TypeScript + Tailwind). — Next.js 16, App Router, `src/`.
- [x] Настроена структура папок. — `src/app` (страницы), `src/lib/supabase` (клиенты), `src/proxy.ts`.
- [x] Подключён Supabase (ключи в переменных окружения `.env.local`). — через `@supabase/ssr`.
- [x] Настроена авторизация email/пароль (Supabase Auth). — регистрация, вход, выход (server actions).
- [x] После входа пользователь попадает на `/dashboard`. — + защита: без входа `/dashboard` → `/login`.
- [x] Приложение запускается локально (`npm run dev`). — http://localhost:3000, сборка без ошибок.
- [x] Git: репозиторий заведён, секреты под `.gitignore`, 2 коммита.
- [ ] **Залить на GitHub** — отложено: `gh` не установлен, нужен репозиторий + `git push`.

## Результат этапа
Проект создан, структура настроена, Supabase подключён, приложение открывается в браузере локально,
работает регистрация/вход/выход, приватные страницы защищены. Проверено end-to-end через браузер.

## Граница с Этапом 2 (решение Андрея)
Этап 1 = **технический фундамент + рабочая авторизация** (страницы-заглушки). Красивая публичная
главная и каркас всех разделов кабинета (`/events`, `/tasks`, `/documents`, `/experience`) — **Этап 2**.

## Статус
`готово ✅` (кроме заливки на GitHub).

## Заметки
- **Подтверждение email** в Supabase сейчас **отключено** (на время разработки, для удобства):
  регистрация сразу пускает в кабинет, без письма. Проверено: регистрация → `/dashboard`.
  **Перед публичным запуском включить обратно** (Supabase → Authentication → Sign In / Providers → Email).
- На случай включённого подтверждения уже готов обработчик `app/src/app/auth/confirm/route.ts`
  (принимает ссылку из письма и открывает сессию) и понятное сообщение при входе с неподтверждённой
  почтой. Тогда же в Supabase → Email Templates → «Confirm signup» ссылку заменить на
  `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email` и задать Site URL в URL Configuration.
- Next.js 16: middleware переименован в `proxy` — защита маршрутов лежит в `app/src/proxy.ts`.
- Vercel при деплое: **Root Directory = `app`**; переменные из `app/.env.example` добавить в настройках Vercel.
- Код залит на GitHub: `https://github.com/ab9214409843-cyber/proect-007` (ветка `main`).
