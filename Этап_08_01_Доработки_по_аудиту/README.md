# Этап 08_01. Доработки по аудиту

## Цель
Закрыть находки независимого аудита (проведён 2026-06-10 на коммите `8e8aa85`, после Этапа 8) —
прежде всего блок «безопасно пустить живых людей». Полный текст аудита и обоснование каждого пункта —
в план-файле `C:\Users\ab921\.claude\plans\frolicking-kindling-sparrow.md`.

## Задача для Claude (новое окно)
> Выполни доработки по аудиту из этого README, начиная с критичного блока (Фаза A). По каждому
> выполненному пункту ставь галочку. Код приложения — только в `app/`. Проверяй `npm run build` и
> `get_advisors` после изменений БД.

## Контекст готовности (из аудита)
- Готовность к MVP (Этапы 1–10): ~70%. К полноценному релизу на тысячи пользователей: ~35–40%.
- К тестированию на себе (Э10) — готово. К запуску живых пользователей и к проду — **не готово**
  без критичного блока ниже.

---

## Чек-лист по приоритетам

### 🔴 Критично — Фаза A (без этого нельзя пускать живых людей)
- [x] **Сброс пароля.** Маршруты `/forgot-password` и `/reset-password`
  (Supabase `resetPasswordForEmail` + `updateUser`), ссылка из письма ведёт через `/auth/confirm`
  (`verifyOtp`, `next=/reset-password`). Ссылка «Забыли пароль?» — на странице входа.
- [~] **Включить защиту авторизации.**
  - [ ] **Leaked-password protection** и **минимальная длина пароля 8** — *настройки в дашборде
    Supabase, делает Андрей* (инструкция ниже в «Заметки»). Код уже рассчитан на 8 символов.
  - **Подтверждение email — отложено до публичного запуска** (решение Андрея: не мешать тестам Э10).
- [x] **Обработка ошибок БД в server actions.** `deleteEvent`, `updateEventStatus`, `deleteDocument`,
  `deleteNote`, `deleteTask`, `updateTaskStatus` теперь проверяют `error` и при сбое показывают
  понятное сообщение (красный бейдж на карточке/в списке).
- [x] **Состояния ошибок/загрузки/не найдено.** Добавлены `error.tsx`, `not-found.tsx`,
  `loading.tsx` (глобальные в `app/src/app/`) + `error.tsx`/`loading.tsx` уровня кабинета.
- [x] **Валидация форм.** Дата окончания не раньше даты начала (`events/actions.ts`); лимиты длины
  (заголовки ≤ 200, тексты ≤ 5000) в server actions + `maxLength` в формах
  (`app/src/lib/validation.ts`).

### 🟡 Желательно до релиза — Фаза B/C
- [ ] **Пагинация + выбор только нужных колонок** в списках `events` / `tasks` / `documents` /
  `experience` (сейчас везде `select("*")` без `.limit()`/`.range()` — на больших объёмах сломается).
- [ ] **Живой дашборд:** ближайшее мероприятие, счётчики, просроченные/ближайшие задачи
  (сейчас `dashboard/page.tsx` статичный, текст «пока это каркас»).
- [ ] **Настройки аккаунта:** имя, смена пароля, удаление аккаунта (поле `users.name` собирается, но
  нигде не используется).
- [ ] **Автоплан при смене даты:** пересобирать сроки задач или предупреждать пользователя
  (сейчас при изменении даты мероприятия задачи остаются со старыми, неверными сроками).
- [ ] **Валидация типа/MIME загружаемого файла**; замена файла документа (сейчас правятся только
  метаданные).
- [ ] **Техдолг:** вынести общий `emptyToNull` (продублирован в 4 файлах actions); `describeSignupError`
  не отдавать сырой английский текст в `default`; добавить логирование ошибок.
- [ ] **CI** (build + lint) на PR + несколько интеграционных тестов на server actions (сейчас тестов 0).

### ⚪ Можно отложить (v1.1 / Этап 11)
- Команды, подрядчики, бюджет, комментарии, уведомления, AI-помощник, автогенерация документов,
  полнотекстовый поиск, продвинутая аналитика, мониторинг-стек.

---

## Результат этапа
Критичный блок (Фаза A) закрыт — приложение можно безопасно дать реальным пользователям и пройти
Этап 10 на живом мероприятии. Пункты Фазы B/C закрываются по мере приоритета.

## Статус
`Фаза A — код готов ✅` (остаётся 2 клика в дашборде Supabase, делает Андрей — см. ниже).
Сборка `npm run build` чистая; новые страницы и экран «не найдено» проверены в браузере.
Фазы B/C — следующие окна.

## Шаги Андрея в дашборде Supabase (Фаза A, осталось)
1. **Защита от утёкших паролей + длина 8.** Authentication → Sign In / Providers → раздел Email
   (или Authentication → Policies): включить **Leaked Password Protection** и поставить
   **Minimum password length = 8**. Это убирает единственное замечание безопасности (`get_advisors`).
2. **Чтобы заработал сброс пароля:**
   - Authentication → URL Configuration → **Redirect URLs**: добавить
     `https://proect-007.vercel.app/**` и `http://localhost:3000/**`.
   - Authentication → Email Templates → **Reset Password**: ссылку привести к виду
     `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery&next=/reset-password`
     (как уже сделано для подтверждения регистрации — единый механизм).

## Заметки
- Полный аудит и обоснования: `C:\Users\ab921\.claude\plans\frolicking-kindling-sparrow.md`.
- План этого этапа (Фаза A): `C:\Users\ab921\.claude\plans\08-01-curious-robin.md`.
- Часть пунктов раньше была намечена в `Этап_10_Тестирование/README.md` (обработка ошибок, валидация
  дат, защита авторизации) — здесь они собраны и приоритизированы; в Э10 остаётся сам тест-сценарий
  Андрея на реальном мероприятии.
- Последовательность: **Фаза A → Фаза B (доделать MVP, Э9–10) → Фаза C (готовность к масштабу)**.
