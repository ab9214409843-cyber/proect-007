// Базовые лимиты длины текстовых полей — общие для форм (атрибут maxLength)
// и server actions (серверная проверка). Защита от случайного «полотна» текста.
export const MAX_TITLE_LEN = 200;
export const MAX_TEXT_LEN = 5000;
